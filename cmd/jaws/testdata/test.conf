general {
  default_profile = "default"
  safe_mode = false
  repo_warn = false
  secrets_path = "secrets"
  editor = "hx"
  gh_token = "GH_TOKEN"
}

manager "aws" "default" {
  access_id = "ACCESS_ID"
  secret_key = "SECRET_KEY"
  region = "AWS_REGION"
}
